{"id":"01KWMS2CTTHTBK1WSGCRFNAXH9","timestamp":"2026-07-03T20:00:09.562994Z","op":"create","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,26 @@\n+---\n+depends_on:\n+- 01KWMS18GCCA8M5SWW0SDX0GAP\n+position_column: todo\n+position_ordinal: 8c80\n+title: Protocol-level cancellation, progress, and per-call timeouts\n+---\n+## What\n+Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n+\n+- [ ] Swift cancel → notifications/cancelled on the wire\n+- [ ] Per-call timeout (configurable), reset by progress\n+- [ ] Progress surfaced to host\n+- [ ] SDK-vs-explicit cancellation path documented\n+\n+## Acceptance Criteria\n+- [ ] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n+- [ ] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not (virtual clock)\n+- [ ] Host observer receives each scripted progress event\n+\n+## Tests\n+- [ ] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset via injected clock, progress event delivery\n+- [ ] `swift test --filter Cancellation` green\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,26 +0,0 @@\n----\n-depends_on:\n-- 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: todo\n-position_ordinal: 8c80\n-title: Protocol-level cancellation, progress, and per-call timeouts\n----\n-## What\n-Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n-\n-- [ ] Swift cancel → notifications/cancelled on the wire\n-- [ ] Per-call timeout (configurable), reset by progress\n-- [ ] Progress surfaced to host\n-- [ ] SDK-vs-explicit cancellation path documented\n-\n-## Acceptance Criteria\n-- [ ] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n-- [ ] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not (virtual clock)\n-- [ ] Host observer receives each scripted progress event\n-\n-## Tests\n-- [ ] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset via injected clock, progress event delivery\n-- [ ] `swift test --filter Cancellation` green\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWQVGA8EXGD5A7HM6JDR0TKJ","timestamp":"2026-07-05T00:40:26.126694Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: todo\n-position_ordinal: 8c80\n+position_column: doing\n+position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: 8c80\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n ## What\n","transaction_id":null}
{"id":"01KWQWMWS7PRCWZXYKKY60X4GF","timestamp":"2026-07-05T01:00:24.743881Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -1,4 +1,28 @@\n ---\n+comments:\n+- actor: claude-code\n+  id: 01kwqwmws7bens6ts6txagp4yz\n+  text: |-\n+    Implementation complete, pending adversarial double-check before final handoff.\n+\n+    What was built (TDD: wrote Tests/FoundationModelsMCPTests/CancellationTests.swift first, confirmed compile-fail RED, then implemented):\n+\n+    1. Sources/FoundationModelsMCP/CallDeadline.swift — new pure `CallDeadline` struct (timeout + resetCount, resetForProgress()). No Clock/Instant state — deliberately, so its \"did a reset happen\" logic is unit-testable without racing real concurrency.\n+\n+    2. Sources/FoundationModelsMCP/MCPServer.swift:\n+       - `call(toolNamed:arguments:timeout:)` now attaches a fresh `ProgressToken`, registers `activeCalls[token]` bookkeeping *before* sending the request (avoids a same-actor-turn race where fast progress could arrive before bookkeeping exists), and wraps the awaited response in `withTaskCancellationHandler` whose `onCancel` fires `client.cancelRequest(_:reason:)` — this is the explicit protocol-level `notifications/cancelled` send, since the swift-sdk does NOT do this automatically on Task cancellation (verified by reading swift-sdk source, documented in docs/swift-sdk-notes.md).\n+       - New `resultOrTimeout(...)` races the real response against a timeout-enforcement loop in a `withThrowingTaskGroup`, comparing `CallDeadline.resetCount` before/after each sleep to detect a progress-driven reset.\n+       - IMPORTANT: the timeout loop uses real `Task.sleep(for:)`, NOT the actor's injectable `clock`. I initially used `clock.sleep`, which broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock()` for unrelated backoff-testing — `ManualClock.sleep` never truly suspends, so the timeout race always \"won\" instantly against genuinely-in-flight calls. Reverted to always-real-wall-clock timing, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). This matches the task's own hint that ManualClock \"doesn't suspend properly for real concurrent racing scenarios.\"\n+       - New `progressUpdates: AsyncStream<CallProgress>` (same continuation-pairing pattern as `catalogUpdates`), fed by `registerProgressHandler()`/`handleProgressNotification(_:)`, mirroring the existing `registerToolListChangedHandler()` once-per-actor registration pattern.\n+       - New `MCPServerError.callTimedOut(toolName:)` case.\n+       - `call()`'s catch clauses are ordered so `CancellationError` and `.callTimedOut` render their own isError result WITHOUT touching `state`/reconnecting (only genuine transport faults do that).\n+\n+    3. docs/swift-sdk-notes.md — appended two sections confirming from swift-sdk source: (a) Task cancellation does NOT auto-propagate to `notifications/cancelled` — `Client.cancelRequest` is the explicit, separate API MCPServer now calls; (b) progress notifications are opt-in per-request via `_meta.progressToken`, with no SDK-level timeout/reset semantics — that policy is entirely `MCPServer`'s own.\n+\n+    Verification: `swift build` clean (no warnings), `swift test` 176/176 passed across 18 suites, `swift test --filter Cancellation` 6/6 passed.\n+\n+    Design judgment calls: added an optional per-call `timeout:` override parameter (not just the host-configurable default) since it fit the existing `call(toolNamed:arguments:)` signature naturally. Kept `CallDeadline` as module-internal (no `public`) since it's a pure implementation detail, testable via `@testable import`. Currently running the really-done adversarial double-check before final handoff.\n+  timestamp: 2026-07-05T01:00:24.743310+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -1,28 +1,4 @@\n ---\n-comments:\n-- actor: claude-code\n-  id: 01kwqwmws7bens6ts6txagp4yz\n-  text: |-\n-    Implementation complete, pending adversarial double-check before final handoff.\n-\n-    What was built (TDD: wrote Tests/FoundationModelsMCPTests/CancellationTests.swift first, confirmed compile-fail RED, then implemented):\n-\n-    1. Sources/FoundationModelsMCP/CallDeadline.swift — new pure `CallDeadline` struct (timeout + resetCount, resetForProgress()). No Clock/Instant state — deliberately, so its \"did a reset happen\" logic is unit-testable without racing real concurrency.\n-\n-    2. Sources/FoundationModelsMCP/MCPServer.swift:\n-       - `call(toolNamed:arguments:timeout:)` now attaches a fresh `ProgressToken`, registers `activeCalls[token]` bookkeeping *before* sending the request (avoids a same-actor-turn race where fast progress could arrive before bookkeeping exists), and wraps the awaited response in `withTaskCancellationHandler` whose `onCancel` fires `client.cancelRequest(_:reason:)` — this is the explicit protocol-level `notifications/cancelled` send, since the swift-sdk does NOT do this automatically on Task cancellation (verified by reading swift-sdk source, documented in docs/swift-sdk-notes.md).\n-       - New `resultOrTimeout(...)` races the real response against a timeout-enforcement loop in a `withThrowingTaskGroup`, comparing `CallDeadline.resetCount` before/after each sleep to detect a progress-driven reset.\n-       - IMPORTANT: the timeout loop uses real `Task.sleep(for:)`, NOT the actor's injectable `clock`. I initially used `clock.sleep`, which broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock()` for unrelated backoff-testing — `ManualClock.sleep` never truly suspends, so the timeout race always \"won\" instantly against genuinely-in-flight calls. Reverted to always-real-wall-clock timing, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). This matches the task's own hint that ManualClock \"doesn't suspend properly for real concurrent racing scenarios.\"\n-       - New `progressUpdates: AsyncStream<CallProgress>` (same continuation-pairing pattern as `catalogUpdates`), fed by `registerProgressHandler()`/`handleProgressNotification(_:)`, mirroring the existing `registerToolListChangedHandler()` once-per-actor registration pattern.\n-       - New `MCPServerError.callTimedOut(toolName:)` case.\n-       - `call()`'s catch clauses are ordered so `CancellationError` and `.callTimedOut` render their own isError result WITHOUT touching `state`/reconnecting (only genuine transport faults do that).\n-\n-    3. docs/swift-sdk-notes.md — appended two sections confirming from swift-sdk source: (a) Task cancellation does NOT auto-propagate to `notifications/cancelled` — `Client.cancelRequest` is the explicit, separate API MCPServer now calls; (b) progress notifications are opt-in per-request via `_meta.progressToken`, with no SDK-level timeout/reset semantics — that policy is entirely `MCPServer`'s own.\n-\n-    Verification: `swift build` clean (no warnings), `swift test` 176/176 passed across 18 suites, `swift test --filter Cancellation` 6/6 passed.\n-\n-    Design judgment calls: added an optional per-call `timeout:` override parameter (not just the host-configurable default) since it fit the existing `call(toolNamed:arguments:)` signature naturally. Kept `CallDeadline` as module-internal (no `public`) since it's a pure implementation detail, testable via `@testable import`. Currently running the really-done adversarial double-check before final handoff.\n-  timestamp: 2026-07-05T01:00:24.743310+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","transaction_id":null}
{"id":"01KWQX4BKT8ZNTEJB5R0XYA32W","timestamp":"2026-07-05T01:08:51.450731Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -29,22 +29,4 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\n-Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n-\n-- [ ] Swift cancel → notifications/cancelled on the wire\n-- [ ] Per-call timeout (configurable), reset by progress\n-- [ ] Progress surfaced to host\n-- [ ] SDK-vs-explicit cancellation path documented\n-\n-## Acceptance Criteria\n-- [ ] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n-- [ ] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not (virtual clock)\n-- [ ] Host observer receives each scripted progress event\n-\n-## Tests\n-- [ ] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset via injected clock, progress event delivery\n-- [ ] `swift test --filter Cancellation` green\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -29,4 +29,22 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+## What\n+Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n+\n+- [ ] Swift cancel → notifications/cancelled on the wire\n+- [ ] Per-call timeout (configurable), reset by progress\n+- [ ] Progress surfaced to host\n+- [ ] SDK-vs-explicit cancellation path documented\n+\n+## Acceptance Criteria\n+- [ ] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n+- [ ] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not (virtual clock)\n+- [ ] Host observer receives each scripted progress event\n+\n+## Tests\n+- [ ] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset via injected clock, progress event delivery\n+- [ ] `swift test --filter Cancellation` green\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWQX58XQTE92SWBM3QZ6S0F8","timestamp":"2026-07-05T01:09:21.463947Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -23,6 +23,16 @@\n\n     Design judgment calls: added an optional per-call `timeout:` override parameter (not just the host-configurable default) since it fit the existing `call(toolNamed:arguments:)` signature naturally. Kept `CallDeadline` as module-internal (no `public`) since it's a pure implementation detail, testable via `@testable import`. Currently running the really-done adversarial double-check before final handoff.\n   timestamp: 2026-07-05T01:00:24.743310+00:00\n+- actor: claude-code\n+  id: 01kwqx58xqbr5qys57wggj576p\n+  text: |-\n+    Adversarial double-check (via really-done) returned REVISE with two findings, both documentation-accuracy issues, no functional defects:\n+\n+    1. Task description said \"virtual clock\" for the timeout tests, but the implementation deliberately uses real wall-clock timing (justified, and empirically necessary — see below). Fixed: updated this task's Acceptance Criteria/Tests checkboxes and added an inline note explaining the real-clock decision and reconciling it with the original wording; checked off all boxes.\n+    2. A code comment in MCPServer.swift claimed the onCancel-closure's unstructured `Task {}` was \"the one deliberate exception\" to the file's no-unmanaged-Task convention, which was inaccurate — `performConnectAttempt(transport:timeout:)` (pre-existing, untouched by this task) already has its own un-joined connect-race Tasks. Fixed: softened the comment to \"one of the few deliberate exceptions... (see also `performConnectAttempt`'s un-joined connect-race Tasks)\".\n+\n+    Re-ran `swift build` (clean) and `swift test` (176/176 passed, 18 suites) after both fixes to confirm nothing broke. Per the really-done skill's bounded-loop guidance (fix findings, re-spawn double-check at most once), both findings were doc/record-keeping only and directly actionable, so I applied the fixes and re-verified rather than re-spawning a second full adversarial pass. Ready for /review.\n+  timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -23,16 +23,6 @@\n\n     Design judgment calls: added an optional per-call `timeout:` override parameter (not just the host-configurable default) since it fit the existing `call(toolNamed:arguments:)` signature naturally. Kept `CallDeadline` as module-internal (no `public`) since it's a pure implementation detail, testable via `@testable import`. Currently running the really-done adversarial double-check before final handoff.\n   timestamp: 2026-07-05T01:00:24.743310+00:00\n-- actor: claude-code\n-  id: 01kwqx58xqbr5qys57wggj576p\n-  text: |-\n-    Adversarial double-check (via really-done) returned REVISE with two findings, both documentation-accuracy issues, no functional defects:\n-\n-    1. Task description said \"virtual clock\" for the timeout tests, but the implementation deliberately uses real wall-clock timing (justified, and empirically necessary — see below). Fixed: updated this task's Acceptance Criteria/Tests checkboxes and added an inline note explaining the real-clock decision and reconciling it with the original wording; checked off all boxes.\n-    2. A code comment in MCPServer.swift claimed the onCancel-closure's unstructured `Task {}` was \"the one deliberate exception\" to the file's no-unmanaged-Task convention, which was inaccurate — `performConnectAttempt(transport:timeout:)` (pre-existing, untouched by this task) already has its own un-joined connect-race Tasks. Fixed: softened the comment to \"one of the few deliberate exceptions... (see also `performConnectAttempt`'s un-joined connect-race Tasks)\".\n-\n-    Re-ran `swift build` (clean) and `swift test` (176/176 passed, 18 suites) after both fixes to confirm nothing broke. Per the really-done skill's bounded-loop guidance (fix findings, re-spawn double-check at most once), both findings were doc/record-keeping only and directly actionable, so I applied the fixes and re-verified rather than re-spawning a second full adversarial pass. Ready for /review.\n-  timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","transaction_id":null}
{"id":"01KWQX63HKQVJFPZC5J7MYZCAC","timestamp":"2026-07-05T01:09:48.723868Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -39,4 +39,22 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+## What\n+Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n+\n+- [x] Swift cancel → notifications/cancelled on the wire\n+- [x] Per-call timeout (configurable), reset by progress\n+- [x] Progress surfaced to host\n+- [x] SDK-vs-explicit cancellation path documented\n+\n+## Acceptance Criteria\n+- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n+- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \"win\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\n+- [x] Host observer receives each scripted progress event\n+\n+## Tests\n+- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\n+- [x] `swift test --filter Cancellation` green (6/6)\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -39,22 +39,4 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\n-Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n-\n-- [x] Swift cancel → notifications/cancelled on the wire\n-- [x] Per-call timeout (configurable), reset by progress\n-- [x] Progress surfaced to host\n-- [x] SDK-vs-explicit cancellation path documented\n-\n-## Acceptance Criteria\n-- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n-- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \"win\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\n-- [x] Host observer receives each scripted progress event\n-\n-## Tests\n-- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\n-- [x] `swift test --filter Cancellation` green (6/6)\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWQXGGC8B9KB87SDGTDD34X6","timestamp":"2026-07-05T01:15:29.544278Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n","transaction_id":null}
{"id":"01KWQXW4S5VECAPFWDXH964J55","timestamp":"2026-07-05T01:21:50.885532Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -39,22 +39,4 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\n-Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n-\n-- [x] Swift cancel → notifications/cancelled on the wire\n-- [x] Per-call timeout (configurable), reset by progress\n-- [x] Progress surfaced to host\n-- [x] SDK-vs-explicit cancellation path documented\n-\n-## Acceptance Criteria\n-- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n-- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \"win\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\n-- [x] Host observer receives each scripted progress event\n-\n-## Tests\n-- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\n-- [x] `swift test --filter Cancellation` green (6/6)\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:116` — Enum case `callTimedOut` uses camelCase, inconsistent with the lowercase pattern in MCPServerState. Rename to `calltimedout` to match MCPServerState's lowercase convention.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:332` — Enum case `timedOut` uses camelCase while the sibling case `result` in the same enum uses lowercase. Enum cases within the same enum should be consistently cased. Either rename `timedOut` to `timedout` to match `result`'s lowercase style, or rename `result` to `Result` to match `timedOut`'s camelCase style. Lowercase is preferred to match the established pattern in MCPServerState.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:619` — faultResult() is a near-verbatim copy of timedOutResult(), cancelledResult(), and notAvailableResult() — all four create identical CallTool.Result error structures, differing only in the error message text. This duplication should be extracted into a single parameterized helper function. Extract a single helper function `private static func errorResult(text: String) -> CallTool.Result` that constructs and returns the error result. Replace all four existing methods with calls to this helper, passing only the message text as a parameter. This eliminates the maintenance burden of keeping four identical structures in sync.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:620` — Guard statement nested 4 levels deep (withThrowingTaskGroup > addTask > while > guard), exceeding the 3-level threshold. While the guard-else-continue pattern is idiomatic, the nesting depth makes the timeout-enforcement loop harder to reason about. Extract the timeout-checking logic into a separate helper method or use a different control-flow pattern (e.g., a tuple of (observedResetCount, result) returned from the task, or restructure the task group to reduce nesting).\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:627` — timedOutResult() is a near-verbatim copy of faultResult(), cancelledResult(), and notAvailableResult(). All four create identical CallTool.Result structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:635` — cancelledResult() is a near-verbatim copy of faultResult(), timedOutResult(), and notAvailableResult(). All four create identical CallTool.Result error structures. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:750` — notAvailableResult() is a near-verbatim copy of faultResult(), timedOutResult(), and cancelledResult(). All four create identical CallTool.Result error structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -39,4 +39,22 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:116` — Enum case `callTimedOut` uses camelCase, inconsistent with the lowercase pattern in MCPServerState. Rename to `calltimedout` to match MCPServerState's lowercase convention.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:332` — Enum case `timedOut` uses camelCase while the sibling case `result` in the same enum uses lowercase. Enum cases within the same enum should be consistently cased. Either rename `timedOut` to `timedout` to match `result`'s lowercase style, or rename `result` to `Result` to match `timedOut`'s camelCase style. Lowercase is preferred to match the established pattern in MCPServerState.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:619` — faultResult() is a near-verbatim copy of timedOutResult(), cancelledResult(), and notAvailableResult() — all four create identical CallTool.Result error structures, differing only in the error message text. This duplication should be extracted into a single parameterized helper function. Extract a single helper function `private static func errorResult(text: String) -> CallTool.Result` that constructs and returns the error result. Replace all four existing methods with calls to this helper, passing only the message text as a parameter. This eliminates the maintenance burden of keeping four identical structures in sync.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:620` — Guard statement nested 4 levels deep (withThrowingTaskGroup > addTask > while > guard), exceeding the 3-level threshold. While the guard-else-continue pattern is idiomatic, the nesting depth makes the timeout-enforcement loop harder to reason about. Extract the timeout-checking logic into a separate helper method or use a different control-flow pattern (e.g., a tuple of (observedResetCount, result) returned from the task, or restructure the task group to reduce nesting).\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:627` — timedOutResult() is a near-verbatim copy of faultResult(), cancelledResult(), and notAvailableResult(). All four create identical CallTool.Result structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:635` — cancelledResult() is a near-verbatim copy of faultResult(), timedOutResult(), and notAvailableResult(). All four create identical CallTool.Result error structures. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:750` — notAvailableResult() is a near-verbatim copy of faultResult(), timedOutResult(), and cancelledResult(). All four create identical CallTool.Result error structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n\n\\ No newline at end of file\n+## What\n+Per plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\n+\n+- [x] Swift cancel → notifications/cancelled on the wire\n+- [x] Per-call timeout (configurable), reset by progress\n+- [x] Progress surfaced to host\n+- [x] SDK-vs-explicit cancellation path documented\n+\n+## Acceptance Criteria\n+- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\n+- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \"win\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\"measured against real wall-clock time — never `clock`\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\n+- [x] Host observer receives each scripted progress event\n+\n+## Tests\n+- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\n+- [x] `swift test --filter Cancellation` green (6/6)\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWQXX913Z6MSPM463BY99B1Y","timestamp":"2026-07-05T01:22:28.003067Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n","transaction_id":null}
{"id":"01KWQY3JZ715T70AKZGXXEVSYC","timestamp":"2026-07-05T01:25:54.791726Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -39,4 +39,4 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:116` — Enum case `callTimedOut` uses camelCase, inconsistent with the lowercase pattern in MCPServerState. Rename to `calltimedout` to match MCPServerState's lowercase convention.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:332` — Enum case `timedOut` uses camelCase while the sibling case `result` in the same enum uses lowercase. Enum cases within the same enum should be consistently cased. Either rename `timedOut` to `timedout` to match `result`'s lowercase style, or rename `result` to `Result` to match `timedOut`'s camelCase style. Lowercase is preferred to match the established pattern in MCPServerState.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:619` — faultResult() is a near-verbatim copy of timedOutResult(), cancelledResult(), and notAvailableResult() — all four create identical CallTool.Result error structures, differing only in the error message text. This duplication should be extracted into a single parameterized helper function. Extract a single helper function `private static func errorResult(text: String) -> CallTool.Result` that constructs and returns the error result. Replace all four existing methods with calls to this helper, passing only the message text as a parameter. This eliminates the maintenance burden of keeping four identical structures in sync.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:620` — Guard statement nested 4 levels deep (withThrowingTaskGroup > addTask > while > guard), exceeding the 3-level threshold. While the guard-else-continue pattern is idiomatic, the nesting depth makes the timeout-enforcement loop harder to reason about. Extract the timeout-checking logic into a separate helper method or use a different control-flow pattern (e.g., a tuple of (observedResetCount, result) returned from the task, or restructure the task group to reduce nesting).\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:627` — timedOutResult() is a near-verbatim copy of faultResult(), cancelledResult(), and notAvailableResult(). All four create identical CallTool.Result structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:635` — cancelledResult() is a near-verbatim copy of faultResult(), timedOutResult(), and notAvailableResult(). All four create identical CallTool.Result error structures. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:750` — notAvailableResult() is a near-verbatim copy of faultResult(), timedOutResult(), and cancelledResult(). All four create identical CallTool.Result error structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n\n\\ No newline at end of file\n+## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:116` — **Non-applicable, investigated.** `callTimedOut` does NOT need renaming. Swift's own casing convention (enforced by this repo's `casing` validator rule) is that enum cases are lowerCamelCase, e.g. `notFound`, `jsonError` — not all-lowercase. Read `MCPServerState`'s actual cases: `case connecting`, `case ready`, `case faulted(String)` — every one is a *single word*, so it is trivially all-lowercase because lowerCamelCase of a single word has no internal capital to show, not because the codebase has a special \\\"lowercase multi-word\\\" convention. There is no multi-word case anywhere in `MCPServerState` to generalize from. Meanwhile `callTimedOut`'s own sibling cases in `MCPServerError` — `notReady(MCPServerState)`, `backoffExhausted(serverName:attempts:lastError:)`, `connectAttemptTimedOut` — are all multi-word and all lowerCamelCase, exactly like `callTimedOut`. The suggested rename to `calltimedout` would violate Swift's actual convention and this file's own established sibling-case style. Left as-is.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:332` — **Non-applicable, investigated.** Same false generalization as the finding above, applied to `CallOutcome`'s `case result(CallTool.Result)` / `case timedOut`. `result` is a single word (lowerCamelCase of one word has no capital to show); `timedOut` is two words and correctly capitalizes the second per lowerCamelCase. Both cases already follow the identical, single casing rule — there is no inconsistency to resolve, and renaming either case (to `timedout` or to `Result`) would itself introduce a violation of Swift's lowerCamelCase-for-enum-cases convention. Left as-is.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:619` (faultResult) — Fixed. Added `private static func errorResult(text: String) -> CallTool.Result` and rewrote `faultResult(for:)` to call it with `\\\"Transport fault: \\\\(error)\\\"`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:620` (guard nested 4 levels: withThrowingTaskGroup > addTask > while > guard) — Fixed. Extracted the timeout-watching while/guard loop out of `resultOrTimeout`'s `group.addTask` closure into a new private actor method `watchForTimeout(progressToken:timeout:) async throws -> CallOutcome`. The task-group closure now just calls `try await self.watchForTimeout(...)` (1 level), and the while/guard live inside the new method at their own top level (2 levels), eliminating the 4-level nesting.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:627` (timedOutResult) — Fixed. Rewrote `timedOutResult(toolName:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(toolName)\\\\\\\" timed out.\\\")`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:635` (cancelledResult) — Fixed. Rewrote `cancelledResult(toolName:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(toolName)\\\\\\\" call was cancelled.\\\")`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:750` (notAvailableResult) — Fixed. Rewrote `notAvailableResult(for:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(name)\\\\\\\" is no longer available.\\\")`.\\n\\nVerification after all fixes: `swift build` clean (no warnings), `swift test` — 176/176 tests passed across 18 suites.\\n\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -39,4 +39,4 @@\n position_ordinal: '80'\n title: Protocol-level cancellation, progress, and per-call timeouts\n ---\n-## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:116` — **Non-applicable, investigated.** `callTimedOut` does NOT need renaming. Swift's own casing convention (enforced by this repo's `casing` validator rule) is that enum cases are lowerCamelCase, e.g. `notFound`, `jsonError` — not all-lowercase. Read `MCPServerState`'s actual cases: `case connecting`, `case ready`, `case faulted(String)` — every one is a *single word*, so it is trivially all-lowercase because lowerCamelCase of a single word has no internal capital to show, not because the codebase has a special \\\"lowercase multi-word\\\" convention. There is no multi-word case anywhere in `MCPServerState` to generalize from. Meanwhile `callTimedOut`'s own sibling cases in `MCPServerError` — `notReady(MCPServerState)`, `backoffExhausted(serverName:attempts:lastError:)`, `connectAttemptTimedOut` — are all multi-word and all lowerCamelCase, exactly like `callTimedOut`. The suggested rename to `calltimedout` would violate Swift's actual convention and this file's own established sibling-case style. Left as-is.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:332` — **Non-applicable, investigated.** Same false generalization as the finding above, applied to `CallOutcome`'s `case result(CallTool.Result)` / `case timedOut`. `result` is a single word (lowerCamelCase of one word has no capital to show); `timedOut` is two words and correctly capitalizes the second per lowerCamelCase. Both cases already follow the identical, single casing rule — there is no inconsistency to resolve, and renaming either case (to `timedout` or to `Result`) would itself introduce a violation of Swift's lowerCamelCase-for-enum-cases convention. Left as-is.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:619` (faultResult) — Fixed. Added `private static func errorResult(text: String) -> CallTool.Result` and rewrote `faultResult(for:)` to call it with `\\\"Transport fault: \\\\(error)\\\"`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:620` (guard nested 4 levels: withThrowingTaskGroup > addTask > while > guard) — Fixed. Extracted the timeout-watching while/guard loop out of `resultOrTimeout`'s `group.addTask` closure into a new private actor method `watchForTimeout(progressToken:timeout:) async throws -> CallOutcome`. The task-group closure now just calls `try await self.watchForTimeout(...)` (1 level), and the while/guard live inside the new method at their own top level (2 levels), eliminating the 4-level nesting.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:627` (timedOutResult) — Fixed. Rewrote `timedOutResult(toolName:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(toolName)\\\\\\\" timed out.\\\")`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:635` (cancelledResult) — Fixed. Rewrote `cancelledResult(toolName:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(toolName)\\\\\\\" call was cancelled.\\\")`.\\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:750` (notAvailableResult) — Fixed. Rewrote `notAvailableResult(for:)` to call `errorResult(text: \\\"Tool \\\\\\\"\\\\(name)\\\\\\\" is no longer available.\\\")`.\\n\\nVerification after all fixes: `swift build` clean (no warnings), `swift test` — 176/176 tests passed across 18 suites.\\n\n\\ No newline at end of file\n+## What\\nPer plan.md M5/Lifecycle: (1) Swift task cancellation of an in-flight tool call **propagates to protocol-level `notifications/cancelled`** (via the SDK where it does this; send explicitly where it doesn't — record which in `docs/swift-sdk-notes.md`) so servers don't run orphaned work. (2) Per-call **timeout** with a host-configurable default; an incoming `notifications/progress` for the call **resets** the timeout. (3) Progress notifications are surfaced to the host as an event stream/callback on `MCPServer`.\\n\\n- [x] Swift cancel → notifications/cancelled on the wire\\n- [x] Per-call timeout (configurable), reset by progress\\n- [x] Progress surfaced to host\\n- [x] SDK-vs-explicit cancellation path documented\\n\\n## Acceptance Criteria\\n- [x] Cancelling the Swift task around call() results in a cancelled notification observed by the scripted server\\n- [x] A call with no progress times out at the configured bound; the same call with periodic scripted progress does not — implemented and tested with **real wall-clock timing** (small millisecond durations), not the injectable `clock`/`ManualClock`. Investigated per this card's own hint: `ManualClock.sleep` never truly suspends (no internal await), so racing it against a genuinely in-flight call makes the timeout side always \\\"win\\\" instantly — confirmed empirically when an initial `clock.sleep`-based implementation broke pre-existing ResilienceTests/LiveCatalogTests that inject `ManualClock` for unrelated backoff testing. Reverted to real `Task.sleep(for:)`, mirroring the established `connectAttemptTimedOut` precedent (\\\"measured against real wall-clock time — never `clock`\\\"). The pure reset/expiry arithmetic (`CallDeadline`) is unit-tested directly with no concurrency at all, per the card's suggested alternative.\\n- [x] Host observer receives each scripted progress event\\n\\n## Tests\\n- [x] `Tests/FoundationModelsMCPTests/CancellationTests.swift`: wire-level cancelled notification, timeout firing/reset (real wall-clock, see note above) and `CallDeadline` unit tests, progress event delivery\\n- [x] `swift test --filter Cancellation` green (6/6)\\n\\n## Workflow\\n- Use `/tdd` — write failing tests first, then implement to make them pass.\\n\\n## Review Findings (2026-07-04 20:15)\\n\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:116` — Enum case `callTimedOut` uses camelCase, inconsistent with the lowercase pattern in MCPServerState. Rename to `calltimedout` to match MCPServerState's lowercase convention.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:332` — Enum case `timedOut` uses camelCase while the sibling case `result` in the same enum uses lowercase. Enum cases within the same enum should be consistently cased. Either rename `timedOut` to `timedout` to match `result`'s lowercase style, or rename `result` to `Result` to match `timedOut`'s camelCase style. Lowercase is preferred to match the established pattern in MCPServerState.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:619` — faultResult() is a near-verbatim copy of timedOutResult(), cancelledResult(), and notAvailableResult() — all four create identical CallTool.Result error structures, differing only in the error message text. This duplication should be extracted into a single parameterized helper function. Extract a single helper function `private static func errorResult(text: String) -> CallTool.Result` that constructs and returns the error result. Replace all four existing methods with calls to this helper, passing only the message text as a parameter. This eliminates the maintenance burden of keeping four identical structures in sync.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:620` — Guard statement nested 4 levels deep (withThrowingTaskGroup > addTask > while > guard), exceeding the 3-level threshold. While the guard-else-continue pattern is idiomatic, the nesting depth makes the timeout-enforcement loop harder to reason about. Extract the timeout-checking logic into a separate helper method or use a different control-flow pattern (e.g., a tuple of (observedResetCount, result) returned from the task, or restructure the task group to reduce nesting).\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:627` — timedOutResult() is a near-verbatim copy of faultResult(), cancelledResult(), and notAvailableResult(). All four create identical CallTool.Result structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:635` — cancelledResult() is a near-verbatim copy of faultResult(), timedOutResult(), and notAvailableResult(). All four create identical CallTool.Result error structures. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n- [ ] `Sources/FoundationModelsMCP/MCPServer.swift:750` — notAvailableResult() is a near-verbatim copy of faultResult(), timedOutResult(), and cancelledResult(). All four create identical CallTool.Result error structures, differing only in message text. Extract to a shared `errorResult(text: String)` helper function as described in the faultResult finding.\\n\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWQY3WR7KC5K1HCKF37S2QJW","timestamp":"2026-07-05T01:26:04.807455Z","op":"update","item_id":"01KWMS2CTTWFDNGH51XP53VST7","forward_patch":"--- original\n+++ modified\n@@ -33,6 +33,22 @@\n\n     Re-ran `swift build` (clean) and `swift test` (176/176 passed, 18 suites) after both fixes to confirm nothing broke. Per the really-done skill's bounded-loop guidance (fix findings, re-spawn double-check at most once), both findings were doc/record-keeping only and directly actionable, so I applied the fixes and re-verified rather than re-spawning a second full adversarial pass. Ready for /review.\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n+- actor: claude-code\n+  id: 01kwqy3wr62qkty061jd5np85a\n+  text: |-\n+    Addressed review findings.\n+\n+    Findings 1-2 (enum casing at MCPServer.swift lines ~116/332): investigated per instructions before touching anything. Read MCPServerState's actual cases — `connecting`, `ready`, `faulted(String)` — all single words, hence trivially all-lowercase; there is no multi-word all-lowercase case anywhere in that enum to generalize a \"lowercase multi-word\" convention from. `callTimedOut` (MCPServerError) and `timedOut` (CallOutcome) are both correctly-cased lowerCamelCase multi-word cases, consistent with their own enum's sibling cases (`notReady`, `backoffExhausted`, `connectAttemptTimedOut` in MCPServerError). Concluded both findings are false generalizations from single-word cases; did NOT rename anything. Recorded the reasoning directly on the task description and checked both off as non-applicable.\n+\n+    Findings 3-7 (errorResult duplication + 4-level guard nesting, all one root cause): fixed.\n+    - Added `private static func errorResult(text: String) -> CallTool.Result` in MCPServer.swift.\n+    - `faultResult(for:)`, `timedOutResult(toolName:)`, `cancelledResult(toolName:)`, `notAvailableResult(for:)` now each just call `errorResult(text:)` with their specific message, eliminating the 4x duplicated `CallTool.Result(content: [.text(...)], isError: true)` construction.\n+    - Extracted the timeout-enforcement while/guard loop out of `resultOrTimeout`'s `withThrowingTaskGroup` addTask closure into a new private actor method `watchForTimeout(progressToken:timeout:) async throws -> CallOutcome`, dropping the nesting from 4 levels (withThrowingTaskGroup > addTask > while > guard) to 1 (addTask calling the method) + 2 (while > guard inside it).\n+\n+    Verified no other callers of the four *Result helpers exist outside MCPServer.swift (grepped Sources and Tests).\n+\n+    `swift build` clean (no warnings). `swift test`: 176/176 tests passed across 18 suites. Leaving task in `doing` for /review.\n+  timestamp: 2026-07-05T01:26:04.806894+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -33,22 +33,6 @@\n\n     Re-ran `swift build` (clean) and `swift test` (176/176 passed, 18 suites) after both fixes to confirm nothing broke. Per the really-done skill's bounded-loop guidance (fix findings, re-spawn double-check at most once), both findings were doc/record-keeping only and directly actionable, so I applied the fixes and re-verified rather than re-spawning a second full adversarial pass. Ready for /review.\n   timestamp: 2026-07-05T01:09:21.463291+00:00\n-- actor: claude-code\n-  id: 01kwqy3wr62qkty061jd5np85a\n-  text: |-\n-    Addressed review findings.\n-\n-    Findings 1-2 (enum casing at MCPServer.swift lines ~116/332): investigated per instructions before touching anything. Read MCPServerState's actual cases — `connecting`, `ready`, `faulted(String)` — all single words, hence trivially all-lowercase; there is no multi-word all-lowercase case anywhere in that enum to generalize a \"lowercase multi-word\" convention from. `callTimedOut` (MCPServerError) and `timedOut` (CallOutcome) are both correctly-cased lowerCamelCase multi-word cases, consistent with their own enum's sibling cases (`notReady`, `backoffExhausted`, `connectAttemptTimedOut` in MCPServerError). Concluded both findings are false generalizations from single-word cases; did NOT rename anything. Recorded the reasoning directly on the task description and checked both off as non-applicable.\n-\n-    Findings 3-7 (errorResult duplication + 4-level guard nesting, all one root cause): fixed.\n-    - Added `private static func errorResult(text: String) -> CallTool.Result` in MCPServer.swift.\n-    - `faultResult(for:)`, `timedOutResult(toolName:)`, `cancelledResult(toolName:)`, `notAvailableResult(for:)` now each just call `errorResult(text:)` with their specific message, eliminating the 4x duplicated `CallTool.Result(content: [.text(...)], isError: true)` construction.\n-    - Extracted the timeout-enforcement while/guard loop out of `resultOrTimeout`'s `withThrowingTaskGroup` addTask closure into a new private actor method `watchForTimeout(progressToken:timeout:) async throws -> CallOutcome`, dropping the nesting from 4 levels (withThrowingTaskGroup > addTask > while > guard) to 1 (addTask calling the method) + 2 (while > guard inside it).\n-\n-    Verified no other callers of the four *Result helpers exist outside MCPServer.swift (grepped Sources and Tests).\n-\n-    `swift build` clean (no warnings). `swift test`: 176/176 tests passed across 18 suites. Leaving task in `doing` for /review.\n-  timestamp: 2026-07-05T01:26:04.806894+00:00\n depends_on:\n - 01KWMS18GCCA8M5SWW0SDX0GAP\n position_column: doing\n","transaction_id":null}
