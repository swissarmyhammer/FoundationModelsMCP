---
comments:
- actor: claude-code
  id: 01kwqrjkbtx10cz5wtvnz7hvx8
  text: |-
    Implemented in Sources/FoundationModelsMCP/MCPServer.swift:
    - `catalogUpdates: AsyncStream<ToolCatalog>` (continuation wired at init) and a single emission point `emitCatalogSnapshot()` that increments `catalogEpoch` and yields — called from `applyConnect` success/failure, `call(toolNamed:)`'s mid-call fault path, and the coalesced re-list.
    - `registerToolListChangedHandler()` registers `client.onNotification(ToolListChangedNotification.self)` exactly once per actor (guarded by `hasRegisteredToolListChangedHandler`, since `MCP.Client.onNotification` appends handlers rather than replacing them — unlike `withMethodHandler`).
    - `tool(named:)` resolves against current `discoveredTools` (nil once removed); `MCPServer.toolNoLongerAvailableResult(named:)` + private `notAvailableResult(for:)` render the "no longer available" `isError` text via `ToolContentRenderer`, mirroring the existing `faultResult(for:)` pattern.

    Design dead-end worth recording: my first coalescing implementation used a background `Task` per notification that polled `toolListChangedGeneration` after `clock.sleep(for:)`, expecting a burst of concurrent notifications to "arrive during the sleep." This failed under `ManualClock`, because `ManualClock.sleep(until:tolerance:)` has no internal `await` and never actually suspends — so the spawned watcher task consistently raced ahead of and "won" against the concurrent notification-delivery pipeline (client message loop → our handler), producing 5 separate re-lists instead of 1, every run. I tried patching `ManualClock` to add `Task.yield()` calls to force cooperative scheduling — this "worked" with 50 yields but was still flaky at 10 yields (majority failures across 8 runs), confirming a fixed yield count is not a robust fix and I reverted `ManualClock.swift` entirely (no diff there).

    The actual fix: `coalesceAndRelist()` now does one initial `clock.sleep(for: toolListChangedCoalesceWindow)` (catches a burst that's fully queued before the watcher even starts), then repeats the **real** `tools/list` discovery round trip (`relistOnce()`) until a full round trip completes with the generation unchanged. Because a `tools/list` round trip is a genuine cross-actor, cross-transport exchange (unlike a clock sleep), it naturally gives concurrently-arriving notifications real scheduling room to be observed before the next stability check — this converged reliably across 10+ repeated runs of `--filter LiveCatalog` and 5 repeated full-suite runs (168 tests, 17 suites), with zero flakiness. Exactly one `catalogUpdates` emission happens at the end, regardless of how many internal discovery round trips it took to converge.

    Verification: `swift build` clean; `swift test --filter LiveCatalog` green (4/4) across 10 repeated runs; `swift test` full suite green (168 tests/17 suites) across 5 repeated runs.
  timestamp: 2026-07-04T23:49:15.258372+00:00
- actor: claude-code
  id: 01kwqs598y9tz3vqxqw5n3wdxm
  text: |-
    Adversarial double-check (via really-done) returned REVISE with two findings, both fixed:

    1. Stale doc-comment references to a method named `performRelist()` (leftover from the design iteration described in the previous comment) — the actual method is `relistOnce()`. Fixed both occurrences in MCPServer.swift's `emitCatalogSnapshot()` and `toolNoLongerAvailableResult(named:)` doc comments. Verified with `grep -rn performRelist . --include=*.swift` → no matches.

    2. Missing test coverage for the "readiness-state changes" emission trigger (the task's own third bullet) — none of the original 4 tests observed a `.faulted` snapshot from a failed reconnect or a mid-call transport fault. Added two tests to LiveCatalogTests.swift:
       - `failedReconnectEmitsFaultedSnapshot` — a reconnect via `FlakyConnectTransport` that fails after a prior successful connect emits a snapshot with `state == .faulted` and a higher epoch.
       - `midCallFaultEmitsFaultedThenReadySnapshots` — a mid-call transport fault (via `RespawningTransport`, mirroring `ResilienceTests`' own technique) emits a `.faulted` snapshot, then a `.ready` snapshot once auto-reconnect heals it, with strictly increasing epochs across all three emissions (connect, fault, reconnect).

    Re-verified after fixes: `swift build` clean; `swift test --filter LiveCatalog` green (6/6) across 9 repeated runs, no flakiness; full `swift test` green (170 tests/17 suites, up from 168 — the 2 new tests).

    Task is green and left in `doing` per /implement's process — ready for /review.
  timestamp: 2026-07-04T23:59:27.518269+00:00
- actor: claude-code
  id: 01kwqtz0arrv368fnx0t1zyyjy
  text: |-
    Fixed all 30 review findings from the 2026-07-04 19:06 checklist:

    Group 1 (28 doc-comment format items, 23 in MCPServer.swift + 5 in LiveCatalogTests.swift): rewrote each flagged doc comment so the first `///` line is a complete, period-terminated summary sentence, moving elaboration to a following paragraph after a blank `///` line. Applied the reviewer's suggested rewrite text in each case (locating the current content by symbol/text match since exact line numbers had drifted from prior edits).

    Group 2 (duplication at MCPServer.swift's applyConnect): extracted the three near-identical `guard generation == connectGeneration else { logger.warning(...); return/throw }` blocks into a shared private helper `logStaleGenerationDiscard(_:error:)` (takes a `Logger.Message` and an optional `Error` to include in metadata). Each of the three call sites keeps its own `guard ... else { logStaleGenerationDiscard(...); return/throw }` — only the logging construction is centralized; control flow (return vs throw) stays at each site since it differs.

    Group 3 (unstructured Task at handleToolListChangedNotification): added a stored actor property `private var coalescingTask: Task<Void, Never>?`, assigned at the call site (`coalescingTask = Task { await self.coalesceAndRelist() }`) instead of a bare unstored `Task { }`, and cleared back to `nil` at the end of `coalesceAndRelist()`. No existing task-handle-storage pattern was found elsewhere in MCPServer to follow; this is a minimal addition — nothing currently needs to cancel it early since `isCoalescingToolListChanged` already guards against a second concurrent watcher.

    Verification: `swift build` clean; `swift test` green — 170 tests, 17 suites, 0 failures.

    Checked off all 30 checklist items. Task left in `doing` per /implement process, ready for /review.
  timestamp: 2026-07-05T00:30:58.904032+00:00
depends_on:
- 01KWMS18GCCA8M5SWW0SDX0GAP
- 01KWMS1NTW01VREEHWT2729TVB
position_column: done
position_ordinal: 8f80
title: 'Live catalog: catalogUpdates stream, coalesced re-list, tool(named:) resolution'
---
## What\nAdd the dynamic half of the catalog to `MCPServer` per plan.md Dynamic discovery: `catalogUpdates: AsyncStream<ToolCatalog>` emitting a new versioned snapshot on (a) **coalesced** `tools/list_changed` re-list (debounce a burst of notifications into one re-list), (b) reconnect (implicit re-list — the returning server may differ), (c) readiness-state changes. Per-server epoch increments monotonically per emission. Add `tool(named:) -> MCPTool?` resolving against the **current** catalog (nil once gone) plus a helper producing the structured \"tool no longer available\" result text for consumers.\n\n- [x] catalogUpdates AsyncStream with epoch monotonicity\n- [x] list_changed burst coalesced into a single re-list\n- [x] Reconnect and state changes emit snapshots\n- [x] tool(named:) call-time resolution + not-available result helper\n\n## Acceptance Criteria\n- [x] 5 rapid scripted list_changed notifications produce exactly 1 re-list and 1 new snapshot (virtual clock)\n- [x] Epochs strictly increase across emissions; snapshots are complete (idempotent — consumable without prior state)\n- [x] After scripted tool removal, tool(named:) returns nil and the helper renders the structured not-available message\n\n## Tests\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift`: coalescing, epoch monotonicity, reconnect-implies-relist, resolution nil-after-removal\n- [x] `swift test --filter LiveCatalog` green\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n## Review Findings (2026-07-04 19:06)\n\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:97` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase ending with a colon that continues to the next line. Reformat the first line to be a complete summary: `/// The default policy: a 10-second per-attempt timeout with a 250-millisecond initial backoff.` Then add elaboration after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:244` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase ending with an em-dash that continues to the next line. Reformat the doc comment so the first line is a complete single-sentence summary ending with a period: `/// The stream of versioned ``ToolCatalog`` snapshots this server emits.` followed by a blank `///` line, then elaboration.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:309` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Connects with automatic retry using exponential backoff up to the maximum number of attempts.` Then add elaboration about the timeout and policy after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:338` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Performs the actual connection work shared by both retry-based and single-attempt connections.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:345` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Restores the server state to ready after a mid-call transport fault.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:360` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Returns the rendered error result for mid-call transport faults.` Then add parameter documentation after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:363` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Calls a tool and renders the result for the model.` Then add details about fault handling and auto-reconnection after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:377` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Attempts a single connection bounded by the specified real wall-clock timeout.` Then add details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:397` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be a complete summary: `/// The tools discovered by the most recent successful connection attempt.` Keep the parameter/return documentation in the proper sections.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:413` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// The discovered tools as FoundationModels.Tool values for use in a session.` Then add elaboration about type-erasure after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:421` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be a complete summary: `/// Resolves a tool name against the current catalog, returning nil if not found.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:433` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase ending with a colon that continues to the next line. Reformat the first line to be complete: `/// The current catalog snapshot with this server's identity, epoch, state, and all discovered tools.` Then add details after a blank `///` line if needed.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:440` — This guard statement is a near-verbatim copy of the same pattern at lines 410 and 428, differing only in message text, metadata content, and throw instead of return. Part of a three-occurrence pattern that should be extracted. Extract into the shared helper (see line 410 finding).\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:463` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Returns the rendered error result text for when a previously-resolved tool is no longer available.` Then add parameter/return documentation in proper sections.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:471` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Computes the exponential backoff delay for the next retry attempt.` Then add parameter/return documentation in proper sections.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:502` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Fetches all tool pages from the server via paginated tools/list.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:508` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Increments the epoch and emits a new catalog snapshot when the server identity is established.` Then add conditions after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:514` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Registers the notification handler for tool list changes.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:523` — Creates an unstructured Task without storing it, violating the rule 'Don't leak unstructured `Task { }`; prefer structured concurrency.'. Store the Task handle so it can be managed: `let coalescingTask = Task { await self.coalesceAndRelist() }`, or refactor to use structured concurrency with a TaskGroup or async let if feasible.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:544` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Coalesces rapid tool list change notifications into a single re-list.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:570` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Re-runs tool discovery once and updates the discovered tools if successful.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:601` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Declares the elicitation capability and registers the request handler.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:619` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Routes a server-initiated elicitation request to the coordinator.` Then add details about enforcement and conversion after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:656` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Serializes the single resumption of a shared CheckedContinuation between two racing tasks.` Then add implementation details after a blank `///` line.\n- [x] `Sources/FoundationModelsMCP/MCPServer.swift:682` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to be complete: `/// Resumes the wrapped continuation with the result, or silently ignores a duplicate resumption attempt.` This captures both cases in one summary sentence.\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift:8` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase ending with a colon that continues to the next line. Reformat to place the summary sentence on the first line with a period: `/// Provides comprehensive coverage for the dynamic half of MCPServer's catalog.` Then add elaboration after a blank `///` line.\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift:21` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Restructure the first line to be a complete sentence: `/// Builds a fresh MCP.Client for a test, with a descriptive name.` Then elaborate after a blank `///` line about the naming being for readability in transport logs.\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift:27` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to end with a period: `/// Collects every ToolCatalog snapshot observed from the server's catalog updates stream.` Then add elaboration about the collection order and relationship to ScriptedServerSelfTests after a blank `///` line.\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift:38` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Restructure the first line to be complete with a period: `/// Polls until at least the specified count of snapshots have been recorded, or until the timeout elapses.` This keeps the complete thought on the first line.\n- [x] `Tests/FoundationModelsMCPTests/LiveCatalogTests.swift:55` — The first line of the doc comment does not follow the required format: it must be a single-sentence summary ending in a period, not a phrase that continues to the next line. Reformat the first line to end with a period: `/// Creates a background task that records all catalog update snapshots from the server.` Then add parameter/return documentation after a blank `///` line.\n